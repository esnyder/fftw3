dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y indent ocaml autoconf automake libtool distcc distcc-pump
  post:
    - ./bootstrap.sh
    - make

test:
  override:
    - make test